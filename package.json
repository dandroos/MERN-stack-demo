{
  "name": "express-mongoose-react-starter",
  "version": "1.0.0",
  "description": "A starter for a MERN stack app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"node -r dotenv/config server.js\"  \"cd client && node-sass --watch ./src/App.scss ./src/App.css \" \"cd client && npm start\""
  },
  "author": "David Andrews",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^4.1.1",
    "config": "^3.2.2",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.6.6"
  }
}
